import React from "react";
import ReactDOM from "react-dom/client";
import ICEPortal from "./App.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <ICEPortal />
  </React.StrictMode>
);
